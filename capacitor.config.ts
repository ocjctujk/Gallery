import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'io.IPhoneCam.com',
  appName: 'IPhoneCam',
  webDir: 'www',
  bundledWebRuntime: false
};

export default config;
